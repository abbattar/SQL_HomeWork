USE join_db;

-- 1 --

SELECT COLOR, COUNT(*) , MARK
FROM AUTO
WHERE MARK = 'BMW'
GROUP BY COLOR

UNION ALL

SELECT COLOR, COUNT(*) , MARK
FROM AUTO
WHERE MARK = 'LADA'
GROUP BY COLOR;

-- 2 --

SELECT DISTINCT MARK,
(
SELECT COUNT(*)
FROM AUTO A1
WHERE A1.MARK != A2.MARK
) AS COUNT_OTHER_AUTO
FROM AUTO A2;

-- 3 --

SELECT * FROM test_a;
SELECT * FROM test_b;

SELECT * FROM test_a

-- NATURAL JOIN - не проходили --

NATURAL LEFT JOIN test_b
WHERE test_b.id IS NULL;